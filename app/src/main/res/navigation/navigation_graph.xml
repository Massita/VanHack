<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/jobsFragment">
    <fragment
        android:id="@+id/jobsFragment"
        android:name="com.massita.vanhack.ui.jobs.JobsFragment"
        android:label="fragment_jobs"
        tools:layout="@layout/fragment_jobs" >
        <action
            android:id="@+id/action_jobsFragment_to_jobDetailFragment"
            app:destination="@id/jobDetailFragment"
            app:popUpTo="@id/jobsFragment" />
    </fragment>
    <fragment
        android:id="@+id/jobDetailFragment"
        android:name="com.massita.vanhack.ui.jobs.JobDetailFragment"
        android:label="JobDetailFragment"
        tools:layout="@layout/fragment_job_detail">
        <argument
            android:name="jobItem"
            android:defaultValue="@null"
            app:nullable="true"
            app:argType="com.massita.vanhack.data.Job" />
    </fragment>
</navigation>